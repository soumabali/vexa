# P6.5 Hardening — Migration + Teams Endpoint Dispatch

**Tanggal:** 2026-06-28
**Branch:** `fix/p6-migration-teams-endpoint` (cabang dari `feat/p6-credential-sharing`)
**Dispatcher:** Ame (Hermes)
**Executor:** Claude Code di `02-application/`

---

## 📜 Konteks untuk Claude Code

Kamu Claude Code, coding executor vexa. WAJIB baca sebelum eksekusi:
1. `02-application/CLAUDE.md`
2. `apps/api/internal/team/service.go` — service sudah real (115 LOC baru)
3. `apps/api/internal/db/migrations/008_credential_sharing.sql` — contoh migration pattern

**Konteks:** P6 baru selesai. Hasilnya bagus (5 commit, 4 task PASS), tapi ada **2 gap kritis**:
1. **TIDAK ADA migration untuk `teams` + `team_members` table** → production crash
2. **TIDAK ADA team handlers** (HTTP layer) → frontend dropdown team kosong

**Misi:** Fix kedua gap ini dengan plan terstruktur. ZERO business logic change.

---

## 🔍 Audit Real (Honesty Check)

| Gap | Dampak | Status Saat Ini |
|-----|--------|-----------------|
| `teams` table tidak ada di migration | `CreateTeam` commented-out INSERT → silent no-op | Stub pattern |
| `team_members` table tidak ada | `AddMember` real DB ops akan crash | nilDB fallback (test only) |
| `TeamHandler` HTTP layer tidak ada | Frontend `/api/teams` 404 | Empty dropdown |
| Frontend share form pakai `{team_id, permissions[]}` | UI sudah benar | Works after fix |

**Files yang harus dibuat:**
- `apps/api/internal/db/migrations/015_teams_and_team_members.sql`
- `apps/api/internal/db/migrations/rollback/ROLLBACK_015.sql`
- `apps/api/internal/api/handlers/teams.go`
- Update `apps/api/internal/api/router.go`
- Update `apps/api/cmd/.../main.go` (atau setup file) — instantiate TeamService + TeamHandler
- Update `apps/web/src/lib/api/teams.ts` (bikin baru)
- Update `apps/web/src/app/vault/share/page.tsx` — populate team dropdown

---

## 🎯 5 Task Terstruktur

### Task 1 — Migration 015: Teams + Team Members

**File baru:** `apps/api/internal/db/migrations/015_teams_and_team_members.sql`

```sql
-- Migration 015: Teams & Team Members
-- Adds support for team-based credential sharing (P6 P6.5 hardening)
-- Run order: 015 (after 014)
-- Rollback: ROLLBACK_015.sql

-- ============================================================
-- Tables: Teams & Team Members
-- ============================================================

CREATE TABLE IF NOT EXISTS teams (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  description TEXT,
  owner_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  is_active BOOLEAN DEFAULT true
);

CREATE INDEX IF NOT EXISTS idx_teams_owner_id ON teams(owner_id);
CREATE INDEX IF NOT EXISTS idx_teams_active ON teams(is_active) WHERE is_active = true;

CREATE TABLE IF NOT EXISTS team_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  team_id UUID NOT NULL REFERENCES teams(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  role VARCHAR(20) NOT NULL DEFAULT 'member'
    CHECK (role IN ('owner','admin','member','viewer')),
  joined_at TIMESTAMPTZ DEFAULT now(),
  added_by UUID REFERENCES users(id),
  CONSTRAINT unique_team_member UNIQUE (team_id, user_id)
);

CREATE INDEX IF NOT EXISTS idx_team_members_team_id ON team_members(team_id);
CREATE INDEX IF NOT EXISTS idx_team_members_user_id ON team_members(user_id);

-- ============================================================
-- RLS Policies (if RLS enabled)
-- ============================================================
-- (sesuaikan dengan 010_rls_policies.sql pattern jika ada)

COMMENT ON TABLE teams IS
  'Team/organization. Owner is a user who created the team. Soft-deleted via is_active=false.';
COMMENT ON TABLE team_members IS
  'Team membership. One row per (team, user). Role determines permission level.';
```

**File baru:** `apps/api/internal/db/migrations/rollback/ROLLBACK_015.sql`
```sql
DROP TABLE IF EXISTS team_members;
DROP TABLE IF EXISTS teams;
```

### Task 2 — Uncomment CreateTeam INSERT

**File:** `apps/api/internal/team/service.go` L105-141

Uncomment SQL `INSERT` statements yang sekarang commented out di `CreateTeam`:
- `INSERT INTO teams (...)` 
- `INSERT INTO team_members (...)` (add owner as first member)

**Acceptance:**
- `CreateTeam` benar-benar persist ke DB
- Audit log entry tetap dipanggil

### Task 3 — Team HTTP Handler

**File baru:** `apps/api/internal/api/handlers/teams.go`

Pattern: mirror `hosts.go` style handler.

Endpoints yang dibutuhkan (sesuai frontend):
- `POST /api/v1/teams` — CreateTeam (body: {name, description})
- `GET /api/v1/teams` — ListTeams (user's teams)
- `GET /api/v1/teams/:id` — GetTeam
- `PATCH /api/v1/teams/:id` — UpdateTeam
- `DELETE /api/v1/teams/:id` — DeleteTeam
- `POST /api/v1/teams/:id/members` — AddMember (body: {user_id, role})
- `DELETE /api/v1/teams/:id/members/:user_id` — RemoveMember
- `PATCH /api/v1/teams/:id/members/:user_id` — UpdateMemberRole (body: {role})
- `GET /api/v1/teams/:id/members` — ListMembers

**PENTING:** Perhatikan audit logger passing pattern (lihat handlers lain seperti `credentials.go`).

### Task 4 — Wire Teams Routes

**File:** `apps/api/internal/api/router.go`

Instantiate TeamService + TeamHandler:
```go
// Cari tempat yang tepat (setelah shareHandler)
teamService := team.NewTeamService(db, auditLogger)
teamHandler := handlers.NewTeamHandler(teamService, auditLogger)

// Daftarkan routes di group authenticated
teams := authenticated.Group("/teams")
{
  teams.POST("", teamHandler.Create)
  teams.GET("", teamHandler.List)
  teams.GET("/:id", teamHandler.Get)
  teams.PATCH("/:id", teamHandler.Update)
  teams.DELETE("/:id", teamHandler.Delete)
  teams.GET("/:id/members", teamHandler.ListMembers)
  teams.POST("/:id/members", teamHandler.AddMember)
  teams.PATCH("/:id/members/:user_id", teamHandler.UpdateMemberRole)
  teams.DELETE("/:id/members/:user_id", teamHandler.RemoveMember)
}
```

Cek apakah import `"github.com/soumabali/vexa/internal/team"` dan `"github.com/soumabali/vexa/internal/api/handlers"` sudah ada di router.go. Jika belum, tambahkan.

### Task 5 — Frontend Teams API Client

**File baru:** `apps/web/src/lib/api/teams.ts`

Pattern: mirror `hosts.ts` style.

```typescript
// Type definitions matching backend
export interface Team {
  id: string;
  name: string;
  description?: string;
  owner_id: string;
  created_at: string;
  updated_at: string;
  is_active: boolean;
}

export interface TeamMember {
  id: string;
  team_id: string;
  user_id: string;
  role: 'owner' | 'admin' | 'member' | 'viewer';
  joined_at: string;
  added_by?: string;
}

export interface CreateTeamInput {
  name: string;
  description?: string;
}

export const teamsApi = {
  list: () => apiRequest<{ teams: Team[] }>('/api/v1/teams').then(d => d.teams),
  get: (id: string) => apiRequest<{ team: Team }>(`/api/v1/teams/${id}`).then(d => d.team),
  create: (input: CreateTeamInput) => apiRequest<{ team: Team }>('/api/v1/teams', { method: 'POST', body: JSON.stringify(input) }).then(d => d.team),
  // ... update, delete, members
};
```

**File update:** `apps/web/src/app/vault/share/page.tsx`
- Load teams list saat page mount (useTeams hook atau langsung fetch)
- Populate dropdown dengan team options
- Send `{team_id, permissions: ['read']}` instead of empty

---

## 🚨 Pitfalls (KRUSIAL)

1. **HANYA edit `02-application/`.** Jangan commit/push/.git di sana.
2. **JANGAN ubah business logic team/service.go** (sudah real, 4 task PASS). Cuma uncomment INSERT di CreateTeam.
3. **JANGAN ubah migration 001-014.** Hanya tambah 015 baru.
4. **Pattern:** Ikuti style migration existing (lihat `008_credential_sharing.sql` atau `014_mfa_backup_codes.sql`).
5. **Foreign keys:** `teams.owner_id` dan `team_members.user_id` reference `users(id)` — PASTIKAN tabel `users` ada di migration 001.
6. **Audit logger passing:** handlers butuh `*audit.Logger`. Cek cara passing di handlers existing (credentials.go pattern).
7. **TypeScript strict mode:** Semua response shape harus typed dengan interface.
8. **Generated file:** `tsconfig.tsbuildinfo` akan modify — di-restore manual oleh Ame, bukan urusan kamu.

---

## ✅ Verification Gates

```bash
# Backend
cd apps/api && go build ./...
cd apps/api && go test ./tests/team/... -v -count=1
cd apps/api && go test ./tests/ -run 'TestTeam|TestShare' -count=1

# Migration
# Test bahwa migration 015 valid syntax (psql dry-run jika ada docker compose up)

# Frontend
cd apps/web && npm run typecheck
cd apps/web && npm run lint
cd apps/web && npm run build
```

---

## 📦 Commit Strategy (5 commit)

```
feat(migration): 015 teams and team_members tables + rollback
feat(team): uncomment CreateTeam INSERT persist to DB
feat(handlers): TeamHandler HTTP layer CRUD + members
wip(teams): wire TeamHandler routes router.go
feat(web): teams API client + vault share team selector
```

---

## 📤 Output

Print summary:
```
=== P6.5 SUMMARY ===
Task 1 (migration): <PASS/FAIL, files created>
Task 2 (CreateTeam INSERT): <PASS/FAIL>
Task 3 (TeamHandler): <PASS/FAIL, #endpoints>
Task 4 (wire routes): <PASS/FAIL, route count>
Task 5 (frontend): <PASS/FAIL, files modified>

Verification:
- go build: PASS/FAIL
- go test team: PASS/FAIL + #tests
- npm typecheck: PASS/FAIL
- npm build: PASS/FAIL
- npm lint: PASS/FAIL

Commits: 5
Files created: <count>
Files modified: <count>
```

Kerjakan tanpa konfirmasi. Ame verifikasi setelah selesai.
